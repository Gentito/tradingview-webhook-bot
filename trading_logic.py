def execute_trade(action, symbol):
    # Replace this with real broker API calls
    print(f"Executing {action.upper()} trade for {symbol}")
    
    # Log or send to broker API here
    return f"{action.upper()} order executed for {symbol}"
